import React from 'react';
// import { useParams } from 'react-router-dom';

const AddEditSemesterPage = () => {
  // const { semesterId } = useParams();
  // const isEditMode = Boolean(semesterId);
  return (
    <div>
      {/* <h1>{isEditMode ? 'Edit Semester' : 'Add New Semester'}</h1> */}
      <h1>Add/Edit Semester</h1>
      <p>Form for semester details will be here.</p>
      {/* Placeholder for form fields */}
    </div>
  );
};
export default AddEditSemesterPage;
